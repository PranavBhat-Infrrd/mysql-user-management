apiVersion: v1
kind: Service
metadata:
  name: {{ include "mysql-user-mgmt.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "mysql-user-mgmt.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "mysql-user-mgmt.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
